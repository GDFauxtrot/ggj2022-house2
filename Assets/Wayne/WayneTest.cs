using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WayneTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //FindObjectOfType<MoneyManager>().AddMoney(9999999);
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Test(){
        Debug.Log("Item Used");
    }
}
